# Supabase config for the prod website (read-only).
# Use the ANON key here (NOT service_role). RLS allows public SELECT on published posts.

VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
